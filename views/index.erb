<h2>Welcome to Pizza Shop</h2>

<dir class="row">
	<% @products.each do |p| %>
	<div class="col-md-3">
		<%= p.title %>
	</div>
	<% end %>
</dir>

<button class="btn btn-primary btn-large" onclick="something()" >Click me</button>